# Minimal production dependencies - only what's absolutely required
# This reduces build time by avoiding unnecessary transitive dependencies

# Core framework
Flask==3.0.3
gunicorn==21.2.0

# Database client
supabase==2.6.0

# AI integration
google-genai>=0.2.0

# Validation (used in utils/validate.py and utils/schemas.py)
pydantic==2.9.2

# HTTP client (used in utils/auth.py for JWKS)
requests==2.32.3

# Flask extensions
Flask-Caching==2.3.0
Flask-Cors==5.0.0

# Environment & Auth
python-dotenv==1.0.1
PyJWT==2.9.0
cryptography==43.0.3

